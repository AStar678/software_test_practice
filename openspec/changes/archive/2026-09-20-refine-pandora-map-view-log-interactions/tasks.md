## 1. 导图布局与信息维护

- [x] 1.1 调整 `潘多拉-交互原型.html` 的主内容区、导图页和四象限高度/溢出规则，并删除导图底部说明卡；在桌面外框及 375×812、390×844、430×932 视口验证页面整体不滚动、四个象限均可见且每个象限能独立滚动。
- [x] 1.2 修正“我被派发未完成”任务项的换行和自适应高度，移除名称或进度备注的裁切；用现有长标题和进度备注任务验证名称、状态、紧急度、截止信息、进度和备注均完整显示且任务之间不重叠。
- [x] 1.3 为上层的公司统一信息实现新增、编辑、删除及删除确认，并在界面和保存处理两层执行最多 10 条限制；验证上层可完成三种操作、满 10 条时无法新增，且中层与下层不出现维护入口。
- [x] 1.4 为个人信息实现新增、编辑、删除及删除确认，在首次变更前物化日志摘录并执行最多 10 条限制；验证三个身份都只能维护本人信息，增删改不改变原始日志，满 10 条时显示上限反馈。

## 2. 响应式类甘特视图

- [x] 2.1 增加视图专用的时间范围相交、边界截断、百分比几何和稳定排序逻辑；用跨度不同、跨度相同但紧急度不同、前两项相同但截止日不同的种子任务验证“跨度降序、紧急度降序、截止日升序”。
- [x] 2.2 将月视图重构为按自然周分行的全宽类甘特时间轴，按任务起止日期绘制跨日/跨周任务段并保留任务名称和描述；验证跨范围任务在边界正确截断、本人负责/本人派发可区分、拥挤位置可通过 `+N` 查看剩余任务。
- [x] 2.3 实现无横向滚动的周视图和日视图，分别以七天和 24 小时为横轴绘制任务泳道；验证周/日范围精确到时刻、任务条从开始连续到结束、文本保持可读且页面 `scrollWidth` 不超过可用宽度。
- [x] 2.4 移除月/周/日分段控件，接入月视图周行空白下钻、周视图日期空白下钻、周/日逐级返回以及周/日前后范围按钮；验证任务条和 `+N` 点击只打开详情而不改变当前视图粒度。
- [x] 2.5 为月视图实现年份调整与 12 个月直接选择面板，并保留、更新视图主体上方的范围/图例/排序说明板块；验证跨年选月正确刷新范围与任务，周和日视图只显示其规定的前后与返回操作。
- [x] 2.6 删除视图页底部“本范围内任务”列表和旧横向自动定位逻辑；遍历三个身份和月/周/日范围，验证页面不再出现该列表、没有左右滑动且每个可见任务仍可从任务条或 `+N` 进入详情。

## 3. 日志单选筛选与回顶

- [x] 3.1 增加按身份派生的日志页单选筛选状态，将“日志”“待我审核”“我提交的自建任务”“我的请示”“待处理请示”与即时命令按钮分组；验证首次进入及切换身份后仅“日志”选中，命令按钮打开表单而不改变筛选状态。
- [x] 3.2 重构日志内容渲染，使其一次只输出当前筛选的列表或空状态，并在审核操作后保留当前筛选和刷新计数；分别以三个身份切换所有可用筛选，验证任何时刻都不会堆叠两个内容列表。
- [x] 3.3 增加仅在日志页向下滚动后出现的回到顶部图标按钮，并在切换筛选时复位滚动位置；验证按钮避开底部导航、可通过键盘触发、点击后回到顶部且保留当前筛选。

## 4. 集成与质量验证

- [x] 4.1 直接打开 `潘多拉-交互原型.html`，以三个演示身份回归任务详情、日志详情、审核、请示和通知入口；验证本次状态与事件调整未扩大权限，也未破坏既有流程。
- [x] 4.2 在桌面以及 375×812、390×844、430×932 视口逐项执行本变更规格场景并检查截图；验证无空白渲染、横向溢出、文本裁切、控件遮挡或不连贯布局。
- [x] 4.3 使用键盘遍历新增、删除、年月选择、时间导航、筛选、任务条和回顶控件；验证焦点可见、可访问名称/单选状态准确、弹层可关闭，且任务归属不只依赖颜色表达。
- [x] 4.4 运行 `openspec validate refine-pandora-map-view-log-interactions --strict` 并修复所有报告问题；验证命令成功退出且全部实现任务均有对应的已执行验收证据。
