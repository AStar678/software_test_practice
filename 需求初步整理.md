## 甲方的核心痛点

以前是全公司在一个共享文档里填写任务、更新任务状态。无任务通知、无权限控制、无任务间关联、无任务日志记录。而且共享文档使用很麻烦，如眼花、误触，维护、归档上也很容易乱套。

## 由痛点导出的核心功能

- 每次成功生效的实际业务变动都生成站内通知。任务包括创建（含待创建审核）、名称与说明修改、开始与截止排期或全天标记修改、紧急程度修改、负责人和上级任务调整、文字进展备注及状态变化、提交或直接完成、创建审核与完成审核结果；日志包括创建、编辑及关联任务变更；请示包括提交和回复；公司与个人信息包括新增、编辑和删除。未改变内容的重复保存、校验失败、无权限、取消、页面切换和已读操作不生成通知。
- 任务通知面向创建者、变动前后的全部负责人和对应审核人；影响父子任务关系或状态汇总时，也通知受影响关联任务的合法相关人员。改派不能漏掉原负责人，同一次操作对同一人只发送一条汇总通知。日志仅通知作者及具有该日志查看权限的相关人员，请示通知发起人与处理人，公司信息通知全员，个人信息只通知本人。
- 用户所称的重要度使用现有“紧急程度”（紧急、高、中、低）表达，不增加另一套字段。紧急性是任务属性，不写入任务标题，也不设“临时紧急”通知类型；紧急程度的任何实际调整都生成“紧急程度修改”通知。
- 通知保存操作者、事件时间、当时的对象标题及可读变更摘要，有权查看的关键字段显示修改前后值。原负责人失去权限时只收到安全的改派摘要，不能获知无权查看的新内容；打开关联对象时重新检查当前权限，删除或失去权限后仍可保留合法历史通知。每人独立维护未读状态，支持单条查看和“全部标为已读”。原型在本次内存会话内实际生成站内通知，临期通知保留演示样例，不要求定时临期扫描、外部推送或后端持久化。
- 下层员工只能看到自己负责的任务；中层可以看到自己部门的任务、派发任务；高层则可以看到全部，但也不能任意修改信息；下层也可以自己给自己创建任务，但是要被审核。
- 任务应该有父子关系，比如高层让中层负责一个任务，中层创建该任务的子任务分配给下层。父任务与子任务是同一种可执行任务：父任务有自己的负责人、排期、状态和执行操作，子任务列表只表达拆解关系与各自的完成状态，父任务不因子任务完成而自动完成，也不用百分比汇总进展。
- 日志记录仅属于个人，可被上级看到；关联任务的标题和跳转入口仍需单独检查任务权限，不能因旧日志可见而显示改派后无权查看任务的新信息。已经打开的通知对象详情也要在权限变化后退出或显示受限提示。

## 我对甲方的需求的理解和修正

### 五个界面

从导航栏上看，是五个主要页面，导图、视图、日志、AI地图、我的。

- “日志”，每个人的日志编辑；下层和中层可以给自己创建任务，但需要被上级审核；高层和中层可以在自己的这个界面派发任务，或创建任务后邀请下属；中层可以向上层发起请示，比如“老板，看看我这个项目完成得合您心意否？”；日志是否写明所属任务，是可选的。
- “视图”是任务的展示，月视图、周视图、日视图，类似甘特图，月视图具体到天，周、日具体到时刻；应该分类显示自己负责的任务和自己派发的任务（尤其对中层），作出视觉差异；显示顺序的问题，按照任务紧急程度、距ddl还剩多少综合排序；紧凑状态下超出数量的任务通过 `+N` 查看，提供“完全展开/收起”按钮，展开后月、周、日视图（含日视图全天区域）都直接显示全部可见任务并允许纵向滚动，切换日期、视图或离开后返回保持展开选择；月视图每周增加独立“查看本周”入口行，点击日期数字直接进入该日的日视图；周视图仅点击星期/日期标题进入日视图，任务条始终打开详情，空白不跳转，日视图仍能逐级返回周、月；任务属性：名称、创建时间（只读）、开始与截止日期/时间、全天标记、紧急程度、完成状态、进展备注、负责人等；全天任务只填写开始与截止日期，并在月、周、日视图中按完整日期范围呈现（首尾日期都包含），日视图放在单独的全天区域而不是伪造时刻；完成时任一负责人可提交完成，需要被审核。
- “导图”其实就是“总得有个首页吧”，没有特别独特的意义，分成四象限。左上是boss统一设置的“公司十大xx“，比如公司十大目标、十大成就之类的；右上是“十大自己被派发的尚未完成的任务”，从任务库自动生成；左下是可由个人编辑的十条信息展示，默认是由“日志”自动生成；右下是当天（和前一天？）自己写的日志，默认时间倒序。（左下和右下如何做出差异？）
- “AI地图”，本质该叫“统计”，集成了任务数据统计（比如员工看自己负责的、上层看公司整体）、部门或公司词云图、AI从任务库分析得出个人或公司的建议（可能参考每个员工填写的mbti）。
- “我的”，未提及。常规几样呗。

### 权限

管理员-上层-中层-下层

- 管理员：负责维护和设置一些总体设置，使用管理端。
- 上层：看到所有人的日志、写自己的日志、看到所有任务、派发任务（向中层或直接向下层）、能给自己创建任务、看到全公司的统计信息、看到全公司的AI分析
- 中层：看到自己部门的日志、写自己的日志、看到自己部门的任务、能派发任务（向下层）、能给自己创建任务（需所属上层审核）、能向直属上层发起请示、看到全部门的统计信息、看到自己的AI分析
- 下层：看到自己一个人的日志、写自己的日志、看到自己负责的任务、不能派发任务、可以自己创建任务（需所属中层审核）、不能向上层或中层发起请示、看到自己的统计信息、看到自己的AI分析

任务修改

- 创建者：可以修改任务信息（名称、说明、开始与截止排期、全天标记、紧急程度、上级任务、负责人），创建时间只读且不随编辑变化。
- 负责人：能更新文字进展备注、提交完成状态，需要创建者审核（自己创建的需要直属上级审核）。
- 完成情况只用离散状态（待创建审核、待开始、进行中、待完成审核、已完成、已拒绝）表达，不使用初始进度、百分比进度、进度条或数值进度更新；负责人首次保存进展备注时，任务从“待开始”进入“进行中”。
- 排期必填校验：开始与截止日期始终必填；非全天任务的开始与截止时刻也必填，清空后必须拒绝保存，不能自动补默认值。开启全天时隐藏时刻输入，关闭后恢复输入及必填校验；截止不得早于开始。
- 选择体验保留独立的负责人选择页面，按可派发部门筛选、按姓名搜索并多选；切换部门保留已选，确认才更新表单，取消不改变原选择。上级任务和日志关联任务支持按任务名称搜索、选择或清空，结果受当前身份权限约束，上级任务选择不得形成自引用或循环。
